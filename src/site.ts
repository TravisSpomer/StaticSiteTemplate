/*
	Write TypeScript here.

	Since this script isn't actually used in the empty site, it isn't referenced by the page layout template by default.
	So if you add code here you'll also want to add this to base.hbs:

	<script src="/site.{{timestamp}}.js"></script>
*/
